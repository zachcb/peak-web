const { colors } = require('tailwindcss/defaultTheme')

module.exports = {
  theme: {
    colors: {
      primary: "#0be881",
      secondary: "#ff5e57",
      ...colors
    },
    extend: {},
  },
  variants: {},
  plugins: [],
};
